## 1. Event Handling

- [x] 1.1 Update `formatEventMessage` for `issues_new` to include full issue details when `auto_spawn` is false
- [x] 1.2 Make `issues_new` delivery use `steer` when `auto_spawn` is false, `followUp` when true
